"""Intensity objects — Phase 2 (VIE-SPEC-REP 1.1.0).

Level-uniform 4-connected regions of a quantized intensity map with deterministic
per-frame identity, exact geometry, counted discards, and a deterministic object
store. Tracking identity across frames is explicitly *not* provided here
(MASTER_PLAN §3.19; Phase 3 owns it).
"""
